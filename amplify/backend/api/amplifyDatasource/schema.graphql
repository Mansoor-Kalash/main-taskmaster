type Task @model {
  id: ID!
  title: String!
  description: String
  status: String
  fileURL : String
  Team: Team @connection(name: "TeamTasks")

}
type Note @model {
  id: ID!
  content: String!
}

type Team @model {
  id: ID!
  name: String!
 Task: [Task] @connection(name: "TeamTasks")
}

